# MARK — local Ollama desktop assistant
# Ollama itself is installed separately from https://ollama.com

# HUD + local audio
PyQt6
numpy
sounddevice
edge-tts
miniaudio
pyttsx3
faster-whisper

# Ollama HTTP client / web + browser actions
requests
beautifulsoup4
duckduckgo-search
playwright

# Desktop automation
pyautogui
pyperclip
pygetwindow

# Vision and screen capture
pillow
opencv-python
mss

# System, files and documents
psutil
send2trash
youtube-transcript-api
python-pptx
openpyxl
pandas
pdfplumber
PyPDF2
python-docx
pydub

# Optional remote dashboard
fastapi
uvicorn[standard]
cryptography
python-multipart
qrcode[pil]

# Optional local wake word (the assistant works without it)
# openwakeword

# Browser / platform extras
plyer

# Windows-only packages (pip skips these on macOS/Linux)
comtypes; sys_platform == "win32"
pycaw; sys_platform == "win32"
win10toast; sys_platform == "win32"
pywinauto; sys_platform == "win32"
pywin32; sys_platform == "win32"
wmi; sys_platform == "win32"
